{
  "name": "mummy",
  "version": "0.0.1",
  "description": "Wrap Svelte components with a Cloudflare POST.",
  "author": "Robert Hall <@arxpoetica>",
  "private": true,
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "dev": "rollup -cw",
    "build": "rm -rf dist && rollup -c",
    "prepare": "npm run build",
    "preview": "dotenv -- wrangler preview --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "svelte": "3.24.1"
  },
  "devDependencies": {
    "@cloudflare/wrangler": "1.10.3",
    "@rollup/plugin-commonjs": "15.0.0",
    "@rollup/plugin-node-resolve": "9.0.0",
    "@rollup/plugin-replace": "2.3.3",
    "dotenv-cli": "3.2.0",
    "rollup": "2.25.0",
    "rollup-plugin-terser": "7.0.0"
  }
}
